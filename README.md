# 1. Clone 你的 repo
git clone https://github.com/你的username/goodmorning-image.git
cd goodmorning-image

# 2. 把 index.html 檔案放進去

# 3. 上傳
git add index.html
git commit -m "Add goodmorning image generator"
git push origin main
